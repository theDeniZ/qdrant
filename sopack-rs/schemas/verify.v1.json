{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sopack.sdarm.org/schemas/verify.v1.json",
  "title": "sopack verify output (v1)",
  "description": "The JSON `sopack verify <pack.sopack> --json` prints to stdout. Exit code is 0 when clean, 3 otherwise (SOPACK-1.0-PLAN.md §3.5). Produced by crates/sopack-cli/src/commands/verify.rs (`VerifyResult`).",
  "type": "object",
  "required": ["pack", "clean", "errors"],
  "additionalProperties": false,
  "properties": {
    "pack": { "type": "string" },
    "clean": { "type": "boolean" },
    "errors": { "type": "array", "items": { "type": "string" } }
  }
}
